services:
  - type: web
    name: duplicate-question-finder
    env: python
    buildCommand: "pip install -r requirements.txt && bash setup.sh"
    startCommand: "streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
    plan: free
